export class MonthlySubscription {
    // @ForeignKey(() => Language)
    // @Column({
    //     type: DataType.INTEGER,
    // })
    // lang_id: number;
    // @BelongsTo(() => Language)
    // language: Language;
}
